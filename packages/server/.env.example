# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Path to SQLite database (created by setup:content script)
DATABASE_URL="file:/path/to/your-content/.data/items.db"

# Path to your content directory
KNOWLEDGE_BASE_PATH="/path/to/your-content"

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Server port (default: 3004)
# TASK_SERVICE_PORT=3004

# =============================================================================
# GMAIL INTEGRATION (Optional)
# =============================================================================
# To enable Gmail integration:
# 1. Create a Google Cloud project at https://console.cloud.google.com
# 2. Enable the Gmail API
# 3. Create OAuth 2.0 credentials (Desktop app type)
# 4. Download credentials.json and save to your content repo's .data/ directory
# 5. Run: pnpm --filter @kw/server gmail:auth
# 6. Complete the OAuth flow in your browser

# Override default credential/token paths (optional)
# GMAIL_CREDENTIALS_PATH="/path/to/credentials.json"
# GMAIL_TOKEN_PATH="/path/to/token.json"
